#include "main.h"

/**
 * positive_or_negative - checks the sign of an integer
 * @i: the integer parameter to be checked
 * Return: is o
 */

void positive_or_negative(int i)
{
	if (i < 0)
		printf("%d is negative", i);
	else
		printf("%d is positive", i);
}
