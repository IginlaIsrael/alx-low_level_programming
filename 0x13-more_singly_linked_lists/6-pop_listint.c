#include "lists.h"

/**
 * pop_listint - Is a function that deletes the head node of a
 * listint_t linked list, and returns the head node’s data (n).
 * @head: THe link to be deleted
 * Return: the head node’s data (n).
 *
 */

int pop_listint(listint_t **head)
{
}
