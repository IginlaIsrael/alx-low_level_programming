#include "lists.h"

/**
 * insert_nodeint_at_index - Is a function that inserts
 * a new node at a given position.
 *  @head: The new list to be inserted
 *  @idx: Is the index where the node is to be inserted
 *  @n: THe number / position where the node is added.
 *  @return: Returns the address of the new node, or NULL if it failed
 */

listint_t *insert_nodeint_at_index(listint_t **head, unsigned int idx, int n)
{
}
